/** 所有常量相关的type */

declare namespace Constant {
  /** 查看更多 */
  export interface ReadMore {
    enTitle: string
    cnTitle: string
    width: string
  }
  /**   左侧导航栏名      */

  export interface NavList {
    name: string
    path: string
  }
}
